const = dictionary{

}

function drawDictionary(){
  let template = ''
  for(let key in dictionary){
    let item = dictionary[key]
    template += ``
    let divElm = document.getElementById("section id")
    divElm.innerHTML = template
  }
}

switch(item we are looking at){
  case 'something' : somethingElse += 10
  break;
  
}